# 010. Prologue Section Revisions — Log

**Date:** 2026-08-08
**Phase:** Phase 9 — Prologue Section Revisions

---

## What Was Done

Shifted the Prologue card and tent decoration at desktop breakpoints, reworked the mobile/tablet illustration crop, and added a new decorative "midground" image anchored to the bottom-left of the section across all breakpoints. Also fixed a process gap: created the missing `session/010-prologue-revisions` branch (work had drifted onto `dev` directly) and committed the pending `maintenance-log.md` carry-forward.

---

## Work Log

- **Session branch:** No `session/010-*` branch existed; two doc edits (`010-session-prompt.md`, `maintenance-log.md`) were sitting uncommitted on `dev`. Created `session/010-prologue-revisions` from `dev` and committed those first.
- **Desktop card shift (1280/1440):** Added `margin-right: var(--space-56)` to `.prologue__card-col` inside a new `min-width: 1280px` block — later merged into the `1024px` block (see below).
- **Mobile/tablet illustration (768/390/320):** `.prologue__illustration-wrap` height `360px → 460px`; `.prologue__illustration-img` height `100% → 170%`, `object-fit: cover` (unchanged).
- **Font check:** Confirmed `.prologue__heading`/`.prologue__body` already use correct semantic tokens (`--font-size-h3-mobile`/`--font-size-h3`, `--font-weight-medium`, `--color-text-heading-on-dark`, etc.), matching the same pattern used in Overview, Gallery, and Retail — no changes needed.
- **1280/1440 refinement:** `.prologue__illustration-wrap` `margin-top: var(--space-32)`; `.prologue__illustration-img` `height: 100%`, `object-fit: contain`; `.prologue__tent` `top: 38px`, `right: 170px`, `z-index: 2` (was `97px`/`235px`, no explicit z-index).
- **1024 breakpoint revision:** User asked to apply the 1280 changes (card shift, illustration margin/fit) down to 1024 as well, but keep the tent hidden at 1024–1279 (only reveal it at 1280+). Restructured: merged the shared properties into the existing `1024px` block and moved the `.prologue__tent` `display: block` + position values into the `1280px` block, verified with Playwright that the tent is `display: none` through 1279px and switches on exactly at 1280px.
- **New midground image:** Copied `prologueMidground.png` (source: `C:\Users\keith\Projects\FoHA\Rot\website\`) into `public/images/prologue-midground.png`. Added as the first child of `#prologue`, decorative (`alt=""`, `aria-hidden="true"`). Initially scoped to 1024px+ only, then changed to display at all six breakpoints per user follow-up.
- **Stacking-context bug:** The midground image (`position: absolute; z-index: -1`) was invisible on first render — `.prologue` has `position: relative` but no `z-index`, so it never established its own stacking context, and the `z-index: -1` child escaped to the document root and rendered behind the whole page. Fixed by adding `z-index: 0` to `.prologue`. Caught via Playwright screenshot + computed-style inspection, not by reading the CSS.
- **Verification method:** Used `npx playwright` (installed ad hoc into the scratchpad directory) throughout to check computed styles and screenshots at each breakpoint, after a manual "looks fine at 1280" report from the user turned out to be based on a UI misread — actual root cause was found via automated inspection, not visual re-check alone.

---

## Files Changed

- `sessions/010-session-prompt.md` — noted the modal lightbox fix was already resolved via maintenance PR #3
- `sessions/maintenance-log.md` — recorded PR #3 merge and stale-memory-folder cleanup note
- `src/style.css` — Prologue: card-col desktop shift, illustration wrap/img sizing at mobile and desktop, tent position/reveal breakpoint, new `.prologue__midground` rule set, added `z-index: 0` to `.prologue`
- `index.html` — added `.prologue__midground` image element
- `public/images/prologue-midground.png` — added

---

## Outcome

Phase 9 definition of done:

- [x] User's Prologue revision requests implemented
- [x] All Prologue CMS fields already connected (no changes needed — `prologue.heading1`, `prologue.paragraph`)
- [x] Prologue section verified at 320px, 390px, 768px, 1024px, 1280px, 1440px (via Playwright computed-style checks and screenshots)
- [x] No new console errors introduced (checked via Playwright `console`/`pageerror` listeners during verification)

---

## Carry-Forwards

- **Stacking-context pattern:** Any future decorative background layer added to a section with only `position: relative` (no `z-index`) needs an explicit `z-index` on that section first, or a negative-`z-index` child will render behind the whole page instead of just the section background. See Work Log above.
- **Verify visually, not just by reading CSS:** Playwright (`npx playwright`, installed ad hoc — not a project dependency) was used this session to catch a bug that a pure code read missed. Worth reaching for again when a user reports "doesn't look right" and the CSS reads correctly on paper.
- **Retail links are still placeholders:** `href="#"` on all three retail links in `index.html`'s fallback markup — flagged for session 011.
