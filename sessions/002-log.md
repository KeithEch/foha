# Session 002 Log — Design System

**Date:** 2026-05-18
**Branch:** dev
**Phase:** 1 — Design System

---

## What was done

- Received `Mode 1.tokens.json` (FOHA DS collection) from user
- Retrieved typography from Figma node `42:13` (home 1440 frame)
- Created `src/design-system/tokens.css` — complete token file covering:
  - Primitive numbers and colors
  - Semantic color tokens (background, border, text)
  - Typography (Alegreya — 3 named styles: p1, menu/menu-hover, h2)
  - Spacing (12 steps: 4–160px)
  - Border widths (7 steps: 0–12px)
  - Border radius (7 steps: 0–16px)
  - Shadow (3 levels: --shadow-2/3/4)
  - Duration (3 levels: --duration-fast/medium/slow)
  - Breakpoints (mobile 375 / tablet 768 / desktop 1024 / wide 1440)
- Updated `src/style.css` — added Google Fonts import (Alegreya 400 & 500) and `@import` for tokens.css; set body defaults using tokens
- Created `DESIGN-SYSTEM.md` — full token reference with tables, intended use, and setup instructions
- Created `src/design-system/preview.html` — visual swatch sheet for all token categories; duration boxes are interactive (hover to compare speeds)
- Updated `ROADMAP.md` — all Phase 1 tasks checked off

## Key decisions

- Shadow tokens named `--shadow-2/3/4` per user specification (Material-style naming)
- Duration uses `--duration-medium` (not `--duration-base`) per user specification
- `menu` and `menu-hover` share identical font properties; colour distinction handled by `--color-text-menu-default/hover`
- Breakpoints noted as reference-only — CSS custom properties cannot be used inside `@media` conditions
- Primitive number variables retained (not collapsed into spacing tokens) to preserve JSON structure

## Session close updates

- Updated `sessions/template-base-prompt.md` — Phase 2 Close gate now commits **and** pushes to `origin/dev` (previously said "do not push")
- Updated `sessions/003-session-prompt.md` — added explicit push step to Closing Steps to match the updated template

## Post-session revisions

Shadows renumbered and trimmed to 3 levels:
- Added `--shadow-1` (low — single layer, `0 1px 4px 0 rgba(0,0,0,0.37)`)
- `--shadow-2` = medium (was low); `--shadow-3` = high (was medium)
- Removed `--shadow-4`

Border widths trimmed — scale now stops at `--border-md` (4px):
- Removed `--border-lg` (6px), `--border-xl` (8px), `--border-xxl` (12px)

## Files changed

- `src/design-system/tokens.css` — new (revised)
- `src/design-system/preview.html` — new (revised)
- `DESIGN-SYSTEM.md` — new (revised)
- `src/style.css` — updated
- `ROADMAP.md` — updated

## Next session

Session 003 — Phase 2: Core Structure & CMS Integration
