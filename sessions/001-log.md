# 001. Environment & Repo Setup — Log

**Date:** 2026-05-13
**Phase:** Phase 0 — Environment & Repo Setup

---

## What Was Done

All Phase 0 tasks completed. Git repo initialized with `main`/`dev` branching strategy, Vite scaffolding set up, folder structure committed, hosting migrated from DreamHost to Netlify, placeholder `index.html` deployed and confirmed live, and deployment workflow documented in `README.md`.

---

## Work Log

- Initialized Git repo with `main` (production) and `dev` (working) branches.
- Scaffolded project with Vite (vanilla HTML/CSS/JS). Chose Vite for speed and simplicity per roadmap recommendation.
- Created folder structure: `/src`, `/public`, `/public/images`, `/cms`, `/dist` (gitignored).
- **Key decision — hosting switch:** DreamHost was originally specified as the host. Switched to Netlify (auto-deploy from GitHub `main`) because the user had already set up a Netlify account. All DreamHost-specific tasks (FTP/SSH setup, `.htaccess` config, GitHub Actions deploy pipeline) were removed from the roadmap. No `.htaccess` file was created.
- Placeholder `index.html` deployed to https://foha.netlify.app/ and confirmed live.
- `README.md` updated to reflect Netlify as host and document the deploy workflow.
- `ROADMAP.md` updated throughout to remove DreamHost references and reflect Netlify.
- Session workflow (`sessions/` folder, templates, index) introduced retroactively at the end of Phase 0.

---

## Files Changed

- `.gitignore` — standard Vite/Node ignores, `/dist` excluded
- `index.html` — Vite entry point with placeholder "coming soon" content
- `package.json` / `package-lock.json` — Vite dependency
- `vite.config.js` — Vite build configuration
- `src/main.js` — JS entry (minimal stub)
- `src/style.css` — global styles (minimal stub)
- `public/.gitkeep` / `public/images/.gitkeep` / `cms/.gitkeep` — folder placeholders
- `README.md` — full rewrite; Netlify deploy workflow documented
- `ROADMAP.md` — DreamHost removed, Netlify added throughout, Phase 0 tasks marked complete

---

## Outcome

All Phase 0 tasks complete. Site is live at https://foha.netlify.app/. Deploy pipeline (GitHub `main` → Netlify auto-build) confirmed working.

**Note:** This log was written retroactively at the start of the session workflow, not during the original work. Details are reconstructed from repo state and conversation history.

---

## Carry-Forwards

- `PROCESSRULES.md` still contains a DreamHost reference ("Hosting: DreamHost (Apache). All server config goes in `.htaccess`.") — should be updated to reflect Netlify before or during Session 002.
- No `netlify.toml` has been created yet. The roadmap notes configuring cache headers via `netlify.toml` in Phase 5 — no action needed until then, but worth knowing it doesn't exist.
